const API_CONFIG = {
  // URL do backend - ALTERE AQUI se necessário
  API_BASE_URL: 'https://hyperliquid-whale-backend.onrender.com',
  
  // Timeout em milissegundos (60 segundos)
  TIMEOUT: 60000,
  
  // Intervalo de atualização automática (30 segundos)
  REFRESH_INTERVAL: 30000,
};

export { API_CONFIG };
export default API_CONFIG;
